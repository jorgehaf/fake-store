"use client";

import React from 'react';
import styles from './styles';

const LoadingLayer = () => (
    <styles.LoadingContainer>
        <styles.Spinner />
    </styles.LoadingContainer>
);

export default LoadingLayer;
